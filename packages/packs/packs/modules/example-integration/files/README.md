# Example Integration Module

This module demonstrates the `integration` kind.

Use it as a template for modules that connect external systems through MCP,
auth configuration, and sync/update workflows.
